apply plugin: 'com.android.library'

android {
    compileSdkVersion 24
    buildToolsVersion "24.0.1"
    publishNonDefault true

    defaultConfig {
        minSdkVersion 8
        targetSdkVersion 23
        versionCode 1
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }

    productFlavors {
        recyclerview {
            versionName ".1"
        }

        abslistview {
            versionName ".1"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
        }

        debug {
            minifyEnabled false
        }
    }
}

dependencies {
    compile 'com.android.support:support-annotations:+'
    recyclerviewCompile("com.android.support:recyclerview-v7:24.2.0")
}
